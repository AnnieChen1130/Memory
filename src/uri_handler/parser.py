def parse(uri: str) -> str:
    """Parse a URI and return a string representation.

    Args:
        uri (str): The URI to parse.

    Returns:
        str: A string representation of the parsed URI.
    """
    return f"Parsed URI: {uri}"
